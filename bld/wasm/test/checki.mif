.nocheck
.before
    @set CHK_CHECK=-@diff -i -b $(name).lst $(name).chk

all: $(name).chk .symbolic
    $(%CHK_CHECK)

$(name).chk: .existsonly
    @set CHK_CHECK=-@diff -i -b $(name).lst $(name).asm
